<Window x:Class="BokiIPTV.App.AddPlaylistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add M3U Playlist" Height="240" Width="520"
        WindowStartupLocation="CenterOwner" Background="{StaticResource Panel}"
        ResizeMode="NoResize">
    <StackPanel Margin="22">
        <TextBlock Text="Add M3U Playlist" Foreground="{StaticResource Text}" FontSize="18" FontWeight="Bold"/>
        <TextBlock Text="Paste an M3U / M3U8 URL, or browse to a local .m3u file."
                   Foreground="{StaticResource TextMuted}" Margin="0,6,0,14" TextWrapping="Wrap"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SourceBox" Grid.Column="0" VerticalContentAlignment="Center"/>
            <Button Grid.Column="1" Content="Browse…" Click="Browse_Click" Margin="8,0,0,0"
                    Padding="12,6" Background="{StaticResource Card}"/>
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
            <Button Content="Cancel" Click="Cancel_Click" Padding="16,7" Margin="0,0,8,0" Background="{StaticResource Card}"/>
            <Button Content="Load" Click="Load_Click" Padding="20,7"/>
        </StackPanel>
    </StackPanel>
</Window>
